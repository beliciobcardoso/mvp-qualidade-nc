'use client'
import { Button } from '@/components/ui/button'

export default function AproveReport() {
  return (
    <Button
      onClick={() => {
        console.log('Help!!!!!!!!!')
      }}
    >
      Aprovar
    </Button>
  )
}
